

;; (defwidget right []
;;   (box
;;    :halign "end"
;;    (pill
;;     (box
;;      (resources)
;;      (volume)
;;      (clock)
;;      (power)
;;      (test1 "abc" "def")))))

(defwidget right []
  (box :class "pill"
       :orientation "h"
       :space-evenly false
       :halign "end"
       (children)))

(defwidget test1 []
  (box
   "test1"
   (children)))

(defwidget test2 []
  (box
   (test1 (children))))
